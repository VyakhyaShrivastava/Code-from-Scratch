#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
    int n,i,sum=0;
    cout<<"enter the limit up to which numbers have to be summed up : ";
    cin>>n;
    for(i=1;i<=n;i++)
    {
      sum = sum + i ;
    }
    cout<< "the sum is : "<< sum <<endl;
    getch();
}
