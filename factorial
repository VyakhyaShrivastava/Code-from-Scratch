#include<stdio.h>
#include<conio.h>
main()
{
    int i,n;
    long fact=1;
    printf("enter the no. whose factorial is to be found : ");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        fact=fact*i;
    }
    printf("factorial = %d",fact);
    getch();
}
