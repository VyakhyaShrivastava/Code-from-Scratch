#include<iostream>
using namespace std;

template < class vya >

vya funct1( vya a, vya b)
{
    return a+b;
}

int main()
{
    double x,y,z;
    cout<<"enter a number : ";
    cin>>x;
    cout<<"enter a string : ";
    cin>>y;
    z=funct1(x,y);
    cout << "z = " << z << endl;
}
